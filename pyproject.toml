[project]
name = "turningpoint"
version = "0.1.0"
description = "MCP-powered AI agent for Solace Event Portal management"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=2.0.0",
    "ollama>=0.6.2",
    "python-dotenv>=1.2.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.25.0",
]
